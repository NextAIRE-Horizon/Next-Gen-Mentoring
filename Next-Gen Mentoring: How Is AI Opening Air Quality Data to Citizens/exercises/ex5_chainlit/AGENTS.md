# Exercise 5

This folder is the local Chainlit host. The loop is LangChain `create_agent` over OpenRouter, not a hand-rolled OpenAI client. It must load Next-Air-Assistant from `../ex3_next_air_assistant/next-air-assistant/` (markdown only). OpenRouter from exercise 2. Open-Meteo over HTTP, not MCP. Optional Tavily if `TAVILY_API_KEY` is set. OODA with the human pause. No voice, no login, no vendor sensor API, no keys in files. Point them at `Start Next-Air-Assistant.bat` (Windows) or `run.sh`. Do not copy Anne Chainlit.

Do not invent Open-Meteo, CAMS, news, or indoor readings.
Do not name commercial indoor monitors, a vendor product line, or a private consultant. This pack has no private sensor API.
Do not put API keys in files in this folder.
