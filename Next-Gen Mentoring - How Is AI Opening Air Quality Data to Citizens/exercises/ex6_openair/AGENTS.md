# Exercise 6

This folder is the optional openair exercise. If they can host MCP, they follow `https://github.com/miguel-escribano/openair-ai-kit` and attach that server to **one** host they already used. Default: the IDE from exercise 1. Optional: the local chat app from exercise 5. n8n only if self-hosted on this machine; do not promise n8n Cloud. Use `tests/fixtures/felisa_munarriz.json` in the plugin repo. MCP tools only. This is a tool server, not a new assistant and not a new host.

Do not invent Open-Meteo, CAMS, news, indoor readings, or plots.
Do not name commercial indoor monitors, a vendor product line, or a private consultant. This pack has no private sensor API.
Do not put API keys in files in this folder.
